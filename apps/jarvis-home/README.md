# jarvis-home (command center HUD)

One image, two routes (+ `/health`, `/api/telemetry`). Replaces gethomepage.

Click rack / service / GPU tiles for a dossier overlay (pods on that node).

Pins: `~/jarvis-infra/VERSION` (`IMAGE` / `IMAGE_TAG`). See INSTALL.txt.
