# Laptop wake listener. Pinned on purpose: this installs onto a machine nobody
# rebuilds often, and an onnxruntime or openwakeword major bump that silently
# changes model loading would break "hey jarvis" with no obvious cause.
# Bump deliberately, then re-run `jarvis-wake.py --doctor` on the laptop.
openwakeword==0.6.0
onnxruntime==1.20.1
sounddevice==0.5.1
numpy==2.1.3
requests==2.32.3
