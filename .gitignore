# secrets — never commit plaintext
secrets/secrets.yaml
secrets/*.yaml
!secrets/*.example.yaml
!secrets/*.sops.yaml
!secrets/README.md
*.sops.yaml.bak
keys.txt
*.age
*.pem
*-key.pem
jarvis-tls/

# runtime
*.tar.gz
/tmp/
.ansible/
*.retry
__pycache__/
.DS_Store

# local clones
/cluster/
.kube/
.env

# apps/jarvis-home/output is the production SSR bundle (Home + /status).
# It MUST be committed. Do not add output/ here.
clickops-export*
